services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: registerpanel
      POSTGRES_USER: registerpanel
      POSTGRES_PASSWORD: registerpanel_dev
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U registerpanel"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
